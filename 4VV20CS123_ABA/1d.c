#include<stdio.h>
#include<stdlib.>
void main()
{
int *arr,i,r;
printf("enter the no of elements");
scanf("%d",&r);
arr=(int*)malloc(r*size of (int));
if(arr=NULL)
{
printf("memory allocation failed");
for(i=0;i<n;i++)
{
scanf("%d",&arr[i];
}
}
printf("the arrray elements arr;)
for(i=0;i<n;i++)
{
printf("%d",arr[i]);
}
free(arr);
}
